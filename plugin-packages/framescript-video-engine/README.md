# FrameScript Video Engine

当前能力：候选抽帧、语音转写、人工确认后的画面与提示词分析。

浏览器只会把当前用户授权的已保存视频交给受信任本机适配器；候选帧必须经人工确认后，才会提交画面分析。抽帧基线仅使用画面差异，人物跟踪、姿态、OCR 与专业镜头检测仍属后续能力。

`timeline`、`compile`、`render`、`export` 仅为预留能力，不可执行，也不代表可以输出 MP4。
